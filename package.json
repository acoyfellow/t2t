{
  "name": "t2t",
  "version": "0.2.5",
  "description": "Fn push-to-talk dictation for macOS: local Whisper transcription + auto-paste into the focused app.",
  "keywords": [
    "tauri",
    "rust",
    "svelte",
    "sveltekit",
    "svelte5",
    "macos",
    "desktop-app",
    "speech-to-text",
    "dictation",
    "push-to-talk",
    "fn-key",
    "whisper",
    "whisper-rs",
    "offline",
    "local-first",
    "clipboard",
    "accessibility",
    "productivity"
  ],
  "private": true,
  "scripts": {
    "dev": "cd desktop && bunx tauri dev",
    "build": "cd desktop && bunx tauri build",
    "preview": "cd desktop && bun run macos:install",
    "dev:web": "cd web && bun dev",
    "deploy:web": "cd web && bun run deploy",
    "release": "npm version patch && git push && git push --tags",
    "release:minor": "npm version minor && git push && git push --tags",
    "release:major": "npm version major && git push && git push --tags"
  }
}
